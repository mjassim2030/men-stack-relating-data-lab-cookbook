<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>CookBook | Home</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <!-- Bootstrap Icons -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" />

  <style>
    body {
      background: linear-gradient(to right, #f8f9fa, #e9ecef);
      min-height: 100vh;
    }
    .hero {
      padding-top: 6rem;
      padding-bottom: 6rem;
    }
    .hero h1 {
      font-weight: 700;
    }
    .hero p {
      font-size: 1.25rem;
    }
    .hero-btn {
      border-radius: 12px;
      padding: 0.75rem 1.5rem;
    }
  </style>
</head>
<body>

  <!-- Navbar -->
  <%- include('./partials/_navbar.ejs') %>

  <!-- Hero Section -->
  <div class="container hero text-center">
    <h1 class="display-4 text-primary">Welcome to the CookBook</h1>
    <p class="text-muted mb-4">Organize your recipes. Manage your ingredients. Cook with confidence.</p>

    <div class="d-flex justify-content-center gap-3">
      <a href="/auth/sign-up" class="btn btn-success hero-btn">
        <i class="bi bi-person-plus-fill me-1"></i> Sign Up
      </a>
      <a href="/auth/sign-in" class="btn btn-outline-primary hero-btn">
        <i class="bi bi-box-arrow-in-right me-1"></i> Sign In
      </a>
    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>